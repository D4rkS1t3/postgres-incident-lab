version: '3.8'

services:
  db:
    image: postgres:15
    container_name: bank_database
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: bank
    ports:
      - "5432:5432"
